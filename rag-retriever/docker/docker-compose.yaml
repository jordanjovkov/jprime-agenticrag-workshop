name: agenticrag-${PROJECT_REVISION}

services:
  grafana:
    image: 'grafana/otel-lgtm:0.25.0'
    container_name: agenticrag-${PROJECT_REVISION}-grafana
    ports:
      - '3000:3000'
      - '4317:4317'
      - '4318:4318'

networks:
  rag-network:
    driver: bridge